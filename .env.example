# ──────────────────────────────────────────────────────────────────────────────
# .env.example — Required environment variables for the Factory Management System
#
# HOW TO USE:
#   1. Copy this file:  cp .env.example .env
#   2. Fill in your real values in .env (never commit .env — it is git-ignored).
#   3. For CI/CD: add each key as a GitHub Secret under
#      Settings → Secrets and variables → Actions, then reference them in
#      .github/workflows/deploy.yml.
#
# All variables must be prefixed with VITE_ so Vite exposes them to the
# client bundle via import.meta.env.VARIABLE_NAME.
# ──────────────────────────────────────────────────────────────────────────────

# Base URL for the Protonest REST API (no trailing slash)
# Used by: src/services/api.js
VITE_API_BASE_URL=

# WebSocket endpoint for real-time sensor data
# Used by: src/services/webSocketClient.js
VITE_WS_URL=

# Protonest account credentials used to acquire an auth token
# Used by: src/services/authService.js
VITE_AUTH_EMAIL=
VITE_AUTH_SECRET_KEY=

# Target device identifier used for stream/state subscriptions and API calls
# Used by: src/services/deviceService.js, src/services/webSocketClient.js
VITE_DEVICE_ID=
